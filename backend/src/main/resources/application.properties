spring.application.name=backend
server.port=8080

# ===============================
# Database Configuration (PostgreSQL)
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/expense_splitter
spring.datasource.username=postgres
spring.datasource.password=moonbreaker123

# Hibernate / JPA settings
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway (DB migrations)
spring.flyway.enabled=true

# ===============================
# JWT Settings
# ===============================
app.jwt.secret=shkfbkfBKFBFBKEWOUFRBG2BEUOFFNBFHB
app.jwt.expiration=86400000

# ===============================
# CORS (Frontend Integration)
# ===============================
spring.web.cors.allowed-origin-patterns=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
